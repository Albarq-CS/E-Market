# E-Market
E-Market is a simple e-commerce web application built with Django. It allows users to browse products and manage the catalog by adding or deleting items, with a details page for each product. The app features a responsive UI using Bootstrap 5, supports image uploads, includes CSRF protection, and uses SQLite with Django Admin for management.
